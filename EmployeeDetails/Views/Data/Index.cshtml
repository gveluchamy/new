@model IEnumerable<EmployeeDetail.Models.Employee>
@{ if (ViewBag.get != null)

    {

        <script type="text/javascript">
            window.onload = function () {
                alert("@ViewBag.get");
            };
        </script>
    }

}



@*<div class="sticky-top">

        @if (User.Identity.IsAuthenticated)
        {

            <h2>  <a asp-action="Privacy"> Go to protected page </a> </h2>

            <form asp-controller="Emp" asp-action="SignOut">

                <button type="submit" name="signOutType" value="app" class="btn btn-primary">Sign Out </button>
                <button type="submit" name="signOutType" value="all" class="btn btn-danger"> Sign Out Completely </button>
            </form>
        }
        else
        {
            <h1>You are not signed in</h1>
            <form asp-controller="Emp" asp-action="SignIn">
                <button type="submit">
                    Sign In
                </button>
            </form>
        }
    </div>
    <br />*@
<hr />
<p>
    <a asp-action="Create" class="btn btn-primary">Create New EmployeeDetails</a>
</p>
<table class="table table-success">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmpID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dob)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>

            <th>
                <h5> Action</h5>
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.EmpID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Gender)
                </td>
                <td>
                    @Convert.ToDateTime(item.Dob).ToString("dd/MM/yyyy")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Department)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>

                <td>


                    @Html.ActionLink("Edit", "Update", new { id = item.EmpID }, new { @class = "btn btn-primary" })   @Html.ActionLink("Delete", "Delete", new { id = item.EmpID }, new { onclick = "return confirm('Are you sure Want to delete?');", @class = "btn btn-danger" })
                    @* @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@

                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}